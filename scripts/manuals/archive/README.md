# Historical Operator-Guide Generators

These scripts are retained as implementation history for superseded or retired
proof batches. They are not part of the portable guide build and intentionally
retain references to the original workstation archive.

Use the maintained generators in the parent directory for current work. Never
use an archived script to overwrite an internal-review candidate.
