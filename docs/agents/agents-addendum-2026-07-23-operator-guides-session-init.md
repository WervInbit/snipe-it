# Operator Guide Session Init - 2026-07-23

## Context

- Reinitialized from `AGENTS.md`, `PROGRESS.md`, `docs/fork-notes.md`, and the active operator-guide generator and AST-02 specification.
- The corrected v2 package remains the review baseline. AC-01 v6, SC-01 v10, and the other reviewed pages are outside this focused change.

## Task

- Revise AST-02 so the unregistered-asset branch is visually grouped with alternative/help routes.
- Keep the six-step registered-asset sequence as the only green primary path.
- Regenerate and visually verify the individual AST-02 proof and combined review PDF.

## Outcome

- AST-02 v5 keeps steps 1-6 as the only primary route.
- The unregistered-asset branch is now a full-width yellow alternative below the other two alternative-route tiles and retains its AST-03 reference chip.
- The latest PNG proof was visually inspected. The individual PDF is one A4 page, the combined PDF remains 13 A4 pages, and no generated HTML contains `dev.inbit`.
- User explicitly approved AC-01 v6, SC-01 v10, and AST-02 v5 for V1. Their specifications and the central approval record now show `Base approved for V1`.
- Packaged WF-01 v5 and WF-02 v3 as the next two-page review batch using only existing evidence. No website or browser interaction occurred because another agent is changing application code.
- The combined workflow PDF was rendered and visually checked; it is two A4 pages and retains WF-02's explicit missing-completion-state evidence block.
- Later in the same guide session, the user confirmed the development site was available and authorized a capture retry. Read-only mobile captures succeeded for the workflow entry, existing run, active cards, expanded instructions, note panel, and photo panel; no application data was changed.
- Generated the superseding workflow review batch v2 with WF-01 v6 on one page and WF-02 v4 on two sides. The revised pages use larger contextual screenshots, an explicit existing-run branch, and separate note/photo evidence steps.
- The three-page combined PDF was rendered with Chrome, re-rendered with Poppler, and visually inspected after crop/callout corrections. The workflow versions remain drafts awaiting user review.
