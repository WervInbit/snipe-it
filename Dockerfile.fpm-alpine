FROM alpine:3.20

RUN printf '%s\n' \
    'Unsupported Dockerfile: this legacy target downloaded official Snipe-IT instead of building this fork.' \
    'Use docker/production/Dockerfile through docker-compose.production.yml.' >&2 \
    && exit 1
