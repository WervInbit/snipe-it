# AST-03 v7 Review

| Field | Value |
| --- | --- |
| Previous version | v6 working draft |
| Feedback source | Owner review, 2026-08-18 |
| Impact | Starting context and dashboard-to-create navigation |
| Status | Working draft; real placement photo pending |

## Correction

- Start page 1 from the expected dashboard state instead of assuming the user
  is already on the hardware index.
- Reuse `DASH-MOBILE-01` as 1A and highlight the `Apparaten` dashboard tile.
- Show `Nieuwe aanmaken` and the toolbar `+` as 1B/1C alternatives within the
  same numbered step.
- Keep the page at four numbered steps so the identity, model, and save
  screenshots retain their readable dimensions.
- Preserve all v6 terminology, save-action, and post-save verification
  corrections.

## QA

- Step 1 reads from left to right: dashboard route, top-bar option, toolbar
  option.
- 1A-1C have separate image identifiers and short captions.
- The alternatives are implied by their grouping and the instruction text;
  no redundant `OF` block is added.
- Both A4 pages remain within their page bounds.
- Exact-version review remains blocked only by the real page-2 step-3 placement
  photo.
